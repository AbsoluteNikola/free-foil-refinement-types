The Language Front
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of Front==

===Literals===
Integer literals //Integer// are nonempty sequences of digits.




VarIdent literals are recognized by the regular expression
`````lower ('_' | digit | letter)*`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in Front are the following:
  | ``bool`` | ``else`` | ``false`` | ``if``
  | ``int`` | ``let`` | ``qualif`` | ``rec``
  | ``true`` | ``val`` |  |

The symbols used in Front are the following:
  | /**@ | ( | ) | :
  | */ | , | { | }
  | => | /*@ | = | ;
  | + | - | * | ==
  | < | <= | > | >=
  | [ | | | ] | ?
  | ' | _ | || | &&

===Comments===
There are no single-line comments in the grammar.There are no multiple-line comments in the grammar.

==The syntactic structure of Front==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Program// | -> | //[Qualifier]// //Term//
  | //Qualifier// | -> | ``/**@`` ``qualif`` //VarIdent// ``(`` //[QualifierArg]// ``)`` ``:`` ``(`` //Pred// ``)`` ``*/``
  | //[Qualifier]// | -> | **eps**
  |  |  **|**  | //Qualifier// //[Qualifier]//
  | //QualifierArg// | -> | //VarIdent// ``:`` //BaseType//
  | //[QualifierArg]// | -> | //QualifierArg//
  |  |  **|**  | //QualifierArg// ``,`` //[QualifierArg]//
  | //Term// | -> | //Integer//
  |  |  **|**  | //ConstBool//
  |  |  **|**  | //VarIdent//
  |  |  **|**  | //VarIdent// ``(`` //[FuncAppArg]// ``)``
  |  |  **|**  | ``if`` ``(`` //FuncAppArg// ``)`` ``{`` //Term// ``}`` ``else`` ``{`` //Term// ``}``
  |  |  **|**  | //Decl// //Term//
  |  |  **|**  | ``(`` //[FunArgName]// ``)`` ``=>`` ``{`` //Term// ``}``
  |  |  **|**  | //FuncAppArg// //IntOp// //FuncAppArg//
  | //ConstBool// | -> | ``true``
  |  |  **|**  | ``false``
  | //Annotation// | -> | ``/*@`` ``val`` //VarIdent// ``:`` //RType// ``*/``
  | //Decl// | -> | //Annotation// ``let`` ``rec`` //VarIdent// ``=`` //Term// ``;``
  |  |  **|**  | //Annotation// ``let`` //VarIdent// ``=`` //Term// ``;``
  |  |  **|**  | ``let`` //VarIdent// ``=`` //Term// ``;``
  | //IntOp// | -> | ``+``
  |  |  **|**  | ``-``
  |  |  **|**  | ``*``
  |  |  **|**  | ``==``
  |  |  **|**  | ``<``
  |  |  **|**  | ``<=``
  |  |  **|**  | ``>``
  |  |  **|**  | ``>=``
  | //RType// | -> | //FuncArg// ``=>`` //RType//
  |  |  **|**  | //BaseType// ``[`` //VarIdent// ``|`` //Pred// ``]``
  |  |  **|**  | //BaseType// ``[`` ``?`` ``]``
  |  |  **|**  | ``'`` //VarIdent//
  |  |  **|**  | //BaseType//
  |  |  **|**  | //RType1//
  | //RType1// | -> | //RType2//
  | //RType2// | -> | //RType3//
  | //RType3// | -> | ``(`` //RType// ``)``
  | //FuncArg// | -> | //VarIdent// ``:`` //RType//
  |  |  **|**  | ``_`` ``:`` //RType//
  | //Pred6// | -> | //VarIdent//
  |  |  **|**  | //ConstBool//
  |  |  **|**  | //Integer//
  |  |  **|**  | ``(`` //Pred// ``)``
  | //Pred1// | -> | //Pred1// ``||`` //Pred2//
  |  |  **|**  | //Pred2//
  | //Pred2// | -> | //Pred2// ``&&`` //Pred3//
  |  |  **|**  | //Pred3//
  | //Pred3// | -> | //Pred3// ``==`` //Pred4//
  |  |  **|**  | //Pred4//
  | //Pred4// | -> | //Pred4// ``<`` //Pred5//
  |  |  **|**  | //Pred4// ``<=`` //Pred5//
  |  |  **|**  | //Pred4// ``>`` //Pred5//
  |  |  **|**  | //Pred4// ``>=`` //Pred5//
  |  |  **|**  | //Pred5//
  | //Pred5// | -> | //Pred5// ``+`` //Pred6//
  |  |  **|**  | //Pred5// ``-`` //Pred6//
  |  |  **|**  | //Pred5// ``*`` //Pred6//
  |  |  **|**  | //Pred6//
  | //Pred// | -> | //Pred1//
  | //BaseType// | -> | ``int``
  |  |  **|**  | ``bool``
  | //FunArgName// | -> | //VarIdent//
  | //[FunArgName]// | -> | //FunArgName//
  |  |  **|**  | //FunArgName// ``,`` //[FunArgName]//
  | //FuncAppArg// | -> | //ConstBool//
  |  |  **|**  | //Integer//
  |  |  **|**  | //VarIdent//
  | //[FuncAppArg]// | -> | //FuncAppArg//
  |  |  **|**  | //FuncAppArg// ``,`` //[FuncAppArg]//



%% File generated by the BNF Converter (bnfc 2.9.5).
