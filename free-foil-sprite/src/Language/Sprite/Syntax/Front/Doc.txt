The Language Front
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of Front==

===Literals===
Integer literals //Integer// are nonempty sequences of digits.





VarIdent literals are recognized by the regular expression
`````lower ('_' | digit | letter)*`````

ConIdent literals are recognized by the regular expression
`````upper ('_' | digit | letter)*`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in Front are the following:
  | ``bool`` | ``else`` | ``false`` | ``if``
  | ``int`` | ``let`` | ``rec`` | ``switch``
  | ``true`` | ``type`` | ``val`` |

The symbols used in Front are the following:
  | /*Q | ( | ) | :
  | */ | , | /*M | {
  | } | => | /*@ | =
  | ; | | | + | -
  | * | == | < | <=
  | > | >= | [ | ]
  | ? | _ | || | &&
  | ' |  |  |

===Comments===
There are no single-line comments in the grammar.There are no multiple-line comments in the grammar.

==The syntactic structure of Front==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Program// | -> | //[Qualifier]// //[Measure]// //[DataType]// //Term//
  | //Qualifier// | -> | ``/*Q`` //VarIdent// ``(`` //[QualifierArg]// ``)`` ``:`` ``(`` //Pred// ``)`` ``*/``
  | //[Qualifier]// | -> | **eps**
  |  |  **|**  | //Qualifier// //[Qualifier]//
  | //QualifierArg// | -> | //VarIdent// ``:`` //RType//
  | //[QualifierArg]// | -> | //QualifierArg//
  |  |  **|**  | //QualifierArg// ``,`` //[QualifierArg]//
  | //Measure// | -> | ``/*M`` //VarIdent// ``:`` //RType// ``*/``
  | //[Measure]// | -> | **eps**
  |  |  **|**  | //Measure// //[Measure]//
  | //MeasureIdent// | -> | //VarIdent//
  |  |  **|**  | //ConIdent//
  | //Term// | -> | //Integer//
  |  |  **|**  | //ConstBool//
  |  |  **|**  | //VarIdent//
  |  |  **|**  | //ConIdent// //ConAppArgs//
  |  |  **|**  | //VarIdent// ``(`` //[FuncAppArg]// ``)``
  |  |  **|**  | ``if`` ``(`` //FuncAppArg// ``)`` ``{`` //Term// ``}`` ``else`` ``{`` //Term// ``}``
  |  |  **|**  | //Decl// //Term//
  |  |  **|**  | ``(`` //[FunArgName]// ``)`` ``=>`` ``{`` //Term// ``}``
  |  |  **|**  | //FuncAppArg// //IntOp// //FuncAppArg//
  |  |  **|**  | ``switch`` ``(`` //VarIdent// ``)`` ``{`` //[SwitchCase]// ``}``
  | //ConstBool// | -> | ``true``
  |  |  **|**  | ``false``
  | //ConAppArgs// | -> | **eps**
  |  |  **|**  | ``(`` //[FuncAppArg]// ``)``
  | //Annotation// | -> | ``/*@`` ``val`` //VarIdent// ``:`` //RType// ``*/``
  | //Decl// | -> | //Annotation// ``let`` ``rec`` //VarIdent// ``=`` //Term// ``;``
  |  |  **|**  | //Annotation// ``let`` //VarIdent// ``=`` //Term// ``;``
  |  |  **|**  | ``let`` //VarIdent// ``=`` //Term// ``;``
  | //SwitchCase// | -> | ``|`` //ConIdent// //SwitchCaseDataConArgs// ``=>`` //Term//
  | //[SwitchCase]// | -> | //SwitchCase//
  |  |  **|**  | //SwitchCase// //[SwitchCase]//
  | //SwitchCaseDataConArgs// | -> | ``(`` //[FunArgName]// ``)``
  |  |  **|**  | **eps**
  | //IntOp// | -> | ``+``
  |  |  **|**  | ``-``
  |  |  **|**  | ``*``
  |  |  **|**  | ``==``
  |  |  **|**  | ``<``
  |  |  **|**  | ``<=``
  |  |  **|**  | ``>``
  |  |  **|**  | ``>=``
  | //Refinement// | -> | ``[`` //VarIdent// ``|`` //Pred// ``]``
  |  |  **|**  | ``[`` ``?`` ``]``
  |  |  **|**  | **eps**
  | //RType// | -> | //FuncArg// ``=>`` //RType//
  |  |  **|**  | //BaseType// //Refinement//
  |  |  **|**  | //VarIdent// //TypeDataArgs// //Refinement//
  |  |  **|**  | ``(`` //RType// ``)``
  | //TypeDataArg// | -> | //RType//
  | //[TypeDataArg]// | -> | //TypeDataArg//
  |  |  **|**  | //TypeDataArg// ``,`` //[TypeDataArg]//
  | //TypeDataArgs// | -> | ``(`` //[TypeDataArg]// ``)``
  |  |  **|**  | **eps**
  | //DataType// | -> | ``type`` //VarIdent// //DataTypeArgs// ``=`` ``|`` //[DataTypeConstructor]// ``;``
  | //[DataType]// | -> | **eps**
  |  |  **|**  | //DataType// //[DataType]//
  | //DataTypeConstructor// | -> | //ConIdent// //DataTypeConstructorArgs// //DataTypeConstructorPredicate//
  | //[DataTypeConstructor]// | -> | //DataTypeConstructor//
  |  |  **|**  | //DataTypeConstructor// ``|`` //[DataTypeConstructor]//
  | //DataTypeConstructorArgs// | -> | ``(`` //[FuncArg]// ``)``
  |  |  **|**  | **eps**
  | //DataTypeConstructorPredicate// | -> | ``=>`` ``[`` //VarIdent// ``|`` //Pred// ``]``
  |  |  **|**  | **eps**
  | //DataTypeArgs// | -> | ``(`` //[TypeVarId]// ``)``
  |  |  **|**  | **eps**
  | //FuncArg// | -> | //VarIdent// ``:`` //RType//
  |  |  **|**  | ``_`` ``:`` //RType//
  | //[FuncArg]// | -> | //FuncArg//
  |  |  **|**  | //FuncArg// ``,`` //[FuncArg]//
  | //Pred6// | -> | //VarIdent//
  |  |  **|**  | //ConstBool//
  |  |  **|**  | //Integer//
  |  |  **|**  | //MeasureIdent// ``(`` //[MeasureArg]// ``)``
  |  |  **|**  | ``(`` //Pred// ``)``
  | //Pred1// | -> | //Pred1// ``||`` //Pred2//
  |  |  **|**  | //Pred2//
  | //Pred2// | -> | //Pred2// ``&&`` //Pred3//
  |  |  **|**  | //Pred3//
  | //Pred3// | -> | //Pred3// ``==`` //Pred4//
  |  |  **|**  | //Pred4//
  | //Pred4// | -> | //Pred4// ``<`` //Pred5//
  |  |  **|**  | //Pred4// ``<=`` //Pred5//
  |  |  **|**  | //Pred4// ``>`` //Pred5//
  |  |  **|**  | //Pred4// ``>=`` //Pred5//
  |  |  **|**  | //Pred5//
  | //Pred5// | -> | //Pred5// ``+`` //Pred6//
  |  |  **|**  | //Pred5// ``-`` //Pred6//
  |  |  **|**  | //Pred5// ``*`` //Pred6//
  |  |  **|**  | //Pred6//
  | //Pred// | -> | //Pred1//
  | //MeasureArg// | -> | //Pred//
  | //[MeasureArg]// | -> | //MeasureArg//
  |  |  **|**  | //MeasureArg// ``,`` //[MeasureArg]//
  | //BaseType// | -> | ``int``
  |  |  **|**  | ``bool``
  |  |  **|**  | //TypeVarId//
  | //TypeVarId// | -> | ``'`` //VarIdent//
  | //[TypeVarId]// | -> | //TypeVarId//
  |  |  **|**  | //TypeVarId// ``,`` //[TypeVarId]//
  | //FunArgName// | -> | //VarIdent//
  | //[FunArgName]// | -> | //FunArgName//
  |  |  **|**  | //FunArgName// ``,`` //[FunArgName]//
  | //FuncAppArg// | -> | //ConstBool//
  |  |  **|**  | //Integer//
  |  |  **|**  | //VarIdent//
  | //[FuncAppArg]// | -> | //FuncAppArg//
  |  |  **|**  | //FuncAppArg// ``,`` //[FuncAppArg]//



%% File generated by the BNF Converter (bnfc 2.9.5).
